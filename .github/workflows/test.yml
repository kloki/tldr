name: Test

env:
  CARGO_TERM_COLOR: always

on: pull_request

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: lintf
        run: cargo-fmt --check
      - name: Run on itself
        run: cargo run ./
